name: Build BOSS JD Bridge APK

on:
  push:
    branches: [boss-jd-apk-build]
    paths:
      - 'boss-jd-bridge/**'
      - '.github/workflows/boss-jd-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35 SDK
        run: yes | sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        working-directory: boss-jd-bridge
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: boss-jd-bridge-apk
          path: boss-jd-bridge/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
